[build]
  publish = "."
  functions = "netlify/functions"

# Force the root URL to invisibly call your Python function
[[redirects]]
  from = "/"
  to = "/.netlify/functions/welcome"
  status = 200
  force = true