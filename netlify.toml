[build]
  publish = "."
  functions = "netlify/functions"

# Redirect the root URL straight to the FastAPI app function
[[redirects]]
  from = "/"
  to = "/.netlify/functions/welcome"
  status = 200
  force = true